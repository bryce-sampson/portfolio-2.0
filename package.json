{
  "name": "portfolio-2.0",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel src/*.html",
    "dev": "parcel src/index.html",
    "build": "parcel build src/index.html",
    "predeploy": "rm -rf dist && parcel build index.html --public-url /portfolio-2.0",
    "deploy": "gh-pages -d dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel-bundler": "^1.12.3"
  }
}
